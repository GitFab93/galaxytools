<tool id="fcs-genome" name="fcs-genome" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="21.05">
    <description>genome assembly QC</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro='requirements' />
    <command detect_errors="exit_code"><![CDATA[
        mkdir -p './outputdir' &&
        ./$fcsadaptor --fasta-input $fasta --output-dir './outputdir' --prok
        && tar -zcf ./outputdir.tgz './outputdir'
]]></command>
    <inputs>
        <param name="fcsadaptor" type="data" label="Run_FCS_adaptor_script" help=""/>
        <param name="fasta" type="data" format="fasta" label="FASTA File" help=""/>
    </inputs>
    <outputs>
        <data name="test" format="txt"/>
        <data name="outputdir" format="tgz" from_work_dir="./outputdir.tgz" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test>
        </test>
    </tests>
    <help><![CDATA[
    help section
    ]]></help>
    <expand macro="citations"/>
</tool>